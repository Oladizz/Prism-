services:
  - type: web
    name: prism
    env: node
    buildCommand: |
      npm install
      cd frontend && npm install && npm run build
      cd ../backend && npm install
    startCommand: cd backend && npx ts-node -r tsconfig-paths/register src/index.ts